// 1. Crea una función que reciba dos números y devuelva su suma

function suma(a, b) {
  return a + b;
}

let sumaDeNumeros = suma(5, 10);
console.log(sumaDeNumeros);

// Función Flecha

let suma1 = (a, b) => a + b;
console.log(suma1(10, 20));
